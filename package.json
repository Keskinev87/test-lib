{
  "name": "game-message-types",
  "version": "1.8.12",
  "description": "Game Message Types",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "private": true,
  "scripts": {
    "build": "npm run clean && npm install && npm audit fix && npm run lint && tsc -p tsconfig.build.json",
    "lint": "eslint --ext .js,.ts .",
    "lint:fix": "eslint --fix --ext .js,.ts .",
    "clean": "rm -rf ./dist ./coverage ./test-report.xml",
    "pushhook": "npm run build"
  },
  "dependencies": {
    "class-validator": "^0.11.1"
  },
  "devDependencies": {
    "@types/node": "^14.0.26",
    "typescript": "^3.9.7",
    "eslint": "^7.5.0",
    "ts-node": "^8.10.2",
    "@typescript-eslint/eslint-plugin": "^3.7.1",
    "@typescript-eslint/parser": "^3.7.1"
  }
}
